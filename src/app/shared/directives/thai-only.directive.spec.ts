import { ThaiOnlyDirective } from './thai-only.directive';

describe('ThaiOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ThaiOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
